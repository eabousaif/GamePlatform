<h2><%= link_to current_player.gamer_tag, player_path(current_player) %>'s Games</h2>

<ol>
  <% @play_sessions.each do |session| %>
    <li><%= link_to session.video_game.name, player_play_sessions_path(current_player, session.video_game) %></li>
    <ul>
      <li>Time Spent Playing - <%= session.duration_in_hours %> hours</li>
    </ul><br />
  <% end %>
 </ol>
 <br />
 <%= link_to "Back", players_path(current_player) %>
<br />
